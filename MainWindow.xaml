<Window x:Class="WordTrainer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WordTrainer"
        Title="WordTrainer — тренажёр слов"
        Height="560" Width="440"
        MinHeight="520" MinWidth="400"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#4F46E5"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#4338CA"/>
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#EEF2FF"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#16A34A"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#DC2626"/>

        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.08" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentSoftBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E7FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="18" Height="18"
                                    Background="White"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="2"
                                    CornerRadius="9">
                                <Ellipse Width="10" Height="10"
                                         Fill="{StaticResource AccentBrush}"
                                         Visibility="{TemplateBinding IsChecked, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Height" Value="36"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>

        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20" Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="WordTrainer"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="{StaticResource AccentBrush}"/>
            <TextBlock Text="Тренируйте английский в своём темпе"
                       FontSize="13"
                       Foreground="{StaticResource TextMutedBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,12" Padding="12,10">
            <UniformGrid Columns="4">
                <StackPanel>
                    <TextBlock x:Name="CorrectCountText" Text="0" Style="{StaticResource StatValue}" Foreground="{StaticResource SuccessBrush}"/>
                    <TextBlock Text="верно" Style="{StaticResource StatLabel}"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock x:Name="WrongCountText" Text="0" Style="{StaticResource StatValue}" Foreground="{StaticResource ErrorBrush}"/>
                    <TextBlock Text="ошибок" Style="{StaticResource StatLabel}"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock x:Name="StreakText" Text="0" Style="{StaticResource StatValue}"/>
                    <TextBlock Text="серия" Style="{StaticResource StatLabel}"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock x:Name="AccuracyText" Text="—" Style="{StaticResource StatValue}"/>
                    <TextBlock Text="точность" Style="{StaticResource StatLabel}"/>
                </StackPanel>
            </UniformGrid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Margin="0,0,0,12" Padding="14,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <RadioButton x:Name="ModeEngRus" Content="EN → RU" GroupName="Mode" IsChecked="True"/>
                    <RadioButton x:Name="ModeRusEng" Content="RU → EN" GroupName="Mode"/>
                </StackPanel>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="DifficultyComboBox"
                              Grid.Column="0"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Value"
                              SelectionChanged="DifficultyComboBox_SelectionChanged"/>
                    <TextBlock x:Name="WordCountTextBlock"
                               Grid.Column="1"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="12"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardBorder}" VerticalAlignment="Stretch">
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="WordTextBlock"
                           Text="—"
                           HorizontalAlignment="Center"
                           FontSize="32"
                           FontWeight="SemiBold"
                           Margin="0,8,0,20"/>

                <TextBox x:Name="TranslationTextBox"
                         KeyDown="TranslationTextBox_KeyDown"
                         IsEnabled="False"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                    <Button x:Name="CheckButton"
                            Content="Проверить"
                            Width="110"
                            Click="CheckButton_Click"
                            IsEnabled="False"/>
                    <Button x:Name="HintButton"
                            Content="Подсказка"
                            Width="110"
                            Style="{StaticResource SecondaryButton}"
                            Click="HintButton_Click"
                            IsEnabled="False"/>
                    <Button x:Name="RandomWordButton"
                            Content="Следующее"
                            Width="110"
                            Click="RandomWordButton_Click"/>
                </StackPanel>

                <TextBlock x:Name="ResultTextBlock"
                           Text="{x:Static local:Constants.PickWordMessage}"
                           HorizontalAlignment="Center"
                           FontSize="15"
                           Margin="0,16,0,8"
                           TextAlignment="Center"
                           Foreground="{StaticResource TextMutedBrush}"/>

                <Button x:Name="ShowTranslationButton"
                        Content="Показать ответ"
                        Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Center"
                        Click="ShowTranslationButton_Click"
                        IsEnabled="False"/>

                <TextBlock x:Name="TranslationTextBlock"
                           HorizontalAlignment="Center"
                           FontSize="17"
                           Margin="0,10,0,0"
                           Foreground="{StaticResource AccentBrush}"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="4"
                   Text="Enter — проверить · Ctrl+N — следующее слово"
                   HorizontalAlignment="Center"
                   Margin="0,12,0,0"
                   FontSize="11"
                   Foreground="{StaticResource TextMutedBrush}"/>
    </Grid>
</Window>
